<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
    <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/theme_color2</item>
        <item name="android:colorPrimaryDark">@color/theme_color1</item>

    </style>

</resources>
